# DuduQ M04–M30 — Matrix update 01

This addendum refines the live preparation matrix without changing any pedagogical content.

## M05/M06 evidence

- M05: v1.1.0 / Pedagogy v1.0 / Canary R124 / systemic-loader-v1; no runtime surface guard or direct-payload bridge; 8 source occurrences of `gap-preview`; 13 planned audio paths with 13 empty active `src` values.
- M06: v1.1.0 / Pedagogy v1.0 / Canary R124 / systemic-loader-v1; no modern guards; 14 source occurrences of `gap-preview`; 18 planned audio paths with 18 empty active `src` values.
- Both contain the same procedural helper family: `svgAsset`, `countDots`, `colorBlock`, `bodyPreview`, `simplePet`, `pairSizePreview`, `simpleSchoolCount`, `iconPreview`, `numeralPreview`, `dayScene`, `greetingScene`.
- In M05, the current effective builder emits Target Shooter for image-choice items and Drag & Drop for sequence items; Smart Sentence names remain in legacy audio/catalog metadata and must not be mistaken for the final runtime contract.
- Canonical pin already contains `big dog`, `small dog`, `big cat`, `small cat`; therefore legacy previews `big-small-dogs` and `big-small-cats` are strong candidates for local composition from existing canonical assets. `brown-dog`, `person-dog`, `person-cat`, `person-rabbit`, `small-white-cat` remain unresolved until visual equivalence is proven.

## Year 2 evidence

- M01–M06 are materialized under `content/english/year-2/module-01` … `module-06` with v2.3 public entries.
- All six use dynamic Player/Loader URLs with `Date.now()` only as observed cache-busting.
- Common chain: three router-compat scripts, bubble smart-renderer bridge, manual-review/mechanics hotfixes and drag-drop visual patch.
- Do not remove this chain by cleanup. The router guards normalize presentation while preserving source answers.
- Word Slash 1.0.17 remains explicitly quarantined by the mechanics-regression layer with reversible Target Shooter fallback.

## Year 3 source-location correction

Year 3 is not merely a policy README. Three relevant GitHub lineages exist:

1. `feat/year3-v23-multimodal` — v2.3 policy/source staging.
2. `scale/shared-engine-year3-bootstrap` at `f33f1a2a2d8d3fdc4b0f89cc388b1e0c86cc8a08` — contains `module-01` … `module-06` plus `year3-content-factory-v1.js`.
3. `scale/year3-shared-engine-foundation` at `44f55de3095d900feeb96d627c77d8b7e13854d6` — contains `module-01` … `module-06` plus factory/plan/visual-resolver files.

Before Year 3 enters FILA A, reconcile these trees against the current v2.3 authority. Do not rebuild from zero and do not assume either scale branch is automatically authoritative.

## Year 4/5 location

No Year 4 or Year 5 implementation was found in current remote branch names, repository code search, or commit-message searches for `Year 4`, `Year 5`, `year4`, or `year5`. Keep M19–M30 fail-closed until a real technical source is located; do not invent modules.

## Automated prep gate

`Global M04-M30 Preparation Audit` run `33449438315`: PASS. Artifact: `global-m04-m30-prep-report` (`9779200764`).
