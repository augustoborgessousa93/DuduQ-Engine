# DUDUQ AI Operating Contract

Before any work, read `.duduq/STATE.json`, `.duduq/HANDOFF.md`, `.duduq/TASKS.md`, and `.duduq/DECISIONS.md`; run `npm run duduq:resume` and `npm run duduq:validate`.

- GitHub is technical continuity; Penpot is visual truth; Core is canonical runtime; mechanics are consumers.
- Never create local Header, Question HUD, shared Fullscreen, Audio, Progress, Button, or Target Select copies.
- Never silently overwrite human work, force-push, merge main, detach Penpot components, delete Gold Masters, or alter gameplay.
- Human browser screenshot is visual approval authority. Penpot live sync must never be claimed without actual capability.
- Target Shooter active Gold Master: Magic Cannon / Magic Launcher.
- Protected Header Main: `88954f25-7a86-800b-8008-a87e6f0d4e44`.
- Protected Question Main: `88954f25-7a86-800b-8008-a87e700a22e9`.
- Control Panel: `855af85f-faf4-8069-8008-a8e75a3255fd`.

Session start: read continuity files, validate branch, inspect Penpot drift, continue `NEXT`.
Session end: validate, update state/handoff/tasks, checkpoint, push development branch when GitHub is available, and mark Penpot sync honestly.

Use `CONTINUAR DUDUQ` as the normal-chat recovery trigger. A fresh executor must recover from GitHub, not conversation memory.
