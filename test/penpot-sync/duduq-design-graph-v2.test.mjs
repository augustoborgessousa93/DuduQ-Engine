import assert from "node:assert/strict";
import {buildGraph,diff,classify,plan} from "../../scripts/duduq-design-graph-v2.mjs";
const base={id:"doc",pages:[{id:"panel",name:"DUDUQ CONTROL PANEL",children:[{id:"screen",name:"MATCHING MASTER IDLE",type:"board",children:[{id:"audio",name:"Audio",type:"rectangle",authoringSource:true,ownership:"CORE_SHARED",x:1,y:2,width:56,height:56,fills:["#349FDF"],shadows:[],assetReference:"speaker"}]}]}]};
const changed=structuredClone(base);changed.pages[0].children[0].children[0].fills=["#8B5CF6"];changed.pages[0].children[0].children[0].x=8;changed.pages[0].children[0].children[0].width=60;changed.pages[0].children[0].children[0].shadows=[{blur:4}];changed.pages[0].children[0].children[0].assetReference="headphones";changed.pages[0].children.push({id:"new",name:"BUBBLE POP MASTER IDLE",type:"board",children:[]});
const a=buildGraph(base,{pageId:"panel"}),b=buildGraph(changed,{pageId:"panel"}),d=diff(a,b),policy={capabilities:{CORE_SHARED:["appearance","geometry","effects","asset"]}};assert.ok(d.some(x=>x.path?.includes("appearance.fills")));assert.ok(d.some(x=>x.path?.includes("geometry.x")));assert.ok(d.some(x=>x.path?.includes("geometry.width")));assert.ok(d.some(x=>x.path?.includes("effects.shadows")));assert.ok(d.some(x=>x.path?.includes("asset.reference")));assert.ok(d.some(x=>x.kind==="NODE_ADDED"));assert.equal(classify(d.find(x=>x.path?.includes("appearance.fills")),a.pages[0].children[0].children[0],policy),"SAFE_AUTO_SYNC");assert.equal(plan([{kind:"NODE_ADDED",nodeId:"unknown"}],b,policy).status,"REVIEW_REQUIRED");console.log("DUDUQ Design Graph V2: PASS");
