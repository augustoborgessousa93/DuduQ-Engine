# DUDUQ TARGET SHOOTER — Gold Master Candidate V1

Isolated visual-validation candidate. It consumes the approved Core exports directly from the Matching Gold Master candidate: GameShell, HUD roles, QuestionPanel, audio/fullscreen buttons, mascots, feedback, CTA attention, and ResultFX semantic `activity-success`.

Target-only code lives in `src/target-shooter-engine.js`, `src/app.js`, and `styles.css`. The source contract is `metadata.targetShooter`; no pedagogical content is embedded in the engine.

Run `node src/target-shooter-engine.test.mjs` for engine coverage. Preview from the workspace server at `/test/target-shooter/gold-master-candidate-v1/`.
