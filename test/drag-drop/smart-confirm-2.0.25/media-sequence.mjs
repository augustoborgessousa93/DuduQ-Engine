import { chromium } from "playwright";

const BASE=process.env.BASE_URL||"http://127.0.0.1:4173";
const URL=`${BASE}/test/drag-drop/smart-confirm-2.0.25/index.html`;
const assert=(c,m)=>{if(!c)throw new Error(m)};
const ROOT="https://raw.githubusercontent.com/augustoborgessousa93/Assets-DuduQ/main/Imagens%20Ilustrativa/";
const IMG={cat:ROOT+"pet-cat-gato.png",dog:ROOT+"pet-dog-cachorro.png",pencil:ROOT+"school-object-pencil-lapis.png",backpack:ROOT+"school-object-backpack-mochila.png"};
const item=id=>`.duduq-dd2-item[data-dd2-item-id="${id}"]`;
const zone=id=>`.duduq-dd2-target[data-dd2-target-id="${id}"] .duduq-dd2-zone`;

const media=()=>({id:"media-gap",title:"MEDIA",instruction:"Classifique as imagens.",behavior:{shuffleItems:false,shuffleTargets:false},payload:{mode:"classification",strategy:"classification",behavior:{shuffleItems:false,shuffleTargets:false},items:[
{id:"cat",label:"CAT",imageUrl:IMG.cat,alt:"Cat",targetId:"pets",required:true},
{id:"dog",label:"DOG",imageUrl:IMG.dog,alt:"Dog",spokenText:"dog",speechLocale:"en-US",audioDescription:"Ouvir dog",targetId:"pets",required:true},
{id:"pencil",label:"PENCIL",imageUrl:IMG.pencil,alt:"Pencil",targetId:"school",required:true},
{id:"backpack",label:"BACKPACK",imageUrl:IMG.backpack,alt:"Backpack",targetId:"school",required:true}],targets:[
{id:"pets",label:"PETS",imageUrl:IMG.dog,alt:"Dog representing pets",spokenText:"pets",speechLocale:"en-US",audioDescription:"Ouvir pets",capacity:2,kind:"category"},
{id:"school",label:"SCHOOL OBJECTS",imageUrl:IMG.backpack,alt:"Backpack representing school objects",capacity:2,kind:"category"}]}});
const sequence=()=>({id:"sequence-gap",title:"SEQUENCE",instruction:"Organize a sequência.",behavior:{shuffleItems:false,shuffleTargets:false},payload:{mode:"sequence",strategy:"sequence",behavior:{shuffleItems:false,shuffleTargets:false},items:[
{id:"C",label:"THIRD",targetId:"line",required:true,sequenceIndex:2},{id:"A",label:"FIRST",targetId:"line",required:true,sequenceIndex:0},{id:"D",label:"FOURTH",targetId:"line",required:true,sequenceIndex:3},{id:"B",label:"SECOND",targetId:"line",required:true,sequenceIndex:1}],targets:[{id:"line",label:"ORDER",capacity:4,kind:"list"}]}});

async function open(browser,name){
 const context=await browser.newContext({viewport:{width:768,height:1024},hasTouch:true,isMobile:false});
 const page=await context.newPage(); await page.emulateMedia({reducedMotion:"reduce"});
 await page.addInitScript(()=>{const state={calls:0,last:""};const synth={speaking:false,pending:false,paused:false,getVoices:()=>[],cancel(){this.speaking=false},pause(){},resume(){},speak(u){state.calls++;state.last=String(u?.text||"");this.speaking=true;try{u?.onstart?.({})}catch{};queueMicrotask(()=>{this.speaking=false;try{u?.onend?.({})}catch{}})}};try{Object.defineProperty(globalThis,"speechSynthesis",{value:synth,configurable:true})}catch{};globalThis.__DD225_TTS__=state;});
 const errors=[],critical404=[]; page.on("pageerror",e=>errors.push(String(e?.message||e))); page.on("response",r=>{if(r.status()===404&&(r.url().includes("/engine/")||r.url().includes("/test/drag-drop/")||r.url().includes("Assets-DuduQ")))critical404.push(r.url())});
 const res=await page.goto(`${URL}?gap=${name}`,{waitUntil:"domcontentloaded",timeout:30000});assert(res?.ok(),`${name}: HTTP ${res?.status()}`);await page.waitForFunction(()=>window.dd225Mechanic?.()?.version==="2.0.25",null,{timeout:15000});return{context,page,errors,critical404};
}
async function mount(page,p){await page.evaluate(x=>window.dd225Mount(x),p);await page.waitForFunction(()=>Boolean(document.querySelector("#mount iframe")?.contentDocument?.querySelector(".duduq-dd2-root")),null,{timeout:15000});await page.waitForFunction(()=>[...(document.querySelector("#mount iframe")?.contentDocument?.querySelectorAll('.duduq-dd2-item')||[])].some(n=>!n.disabled),null,{timeout:15000});await page.evaluate(()=>{__DD225_RESULTS__=[];__DD225_COMPLETIONS__=[]});}
async function snap(page){return page.evaluate(()=>{const d=document.querySelector("#mount iframe")?.contentDocument,l={};d?.querySelectorAll('.duduq-dd2-item[data-dd2-item-id]')?.forEach(n=>{const id=n.getAttribute('data-dd2-item-id');l[id]=n.closest('[data-dd2-target-id]')?.getAttribute('data-dd2-target-id')||'bank'});return{locations:l,results:__DD225_RESULTS__.slice(),confirm:d?.querySelectorAll('.duduq-dd2-confirm').length||0,complete:__DD225_COMPLETIONS__.length,overflow:d?Math.max(0,d.body.scrollWidth-d.documentElement.clientWidth):999}})}
async function selectTap(frame,id){const x=frame.locator(item(id)).first();await x.tap({force:true});await frame.locator(`${item(id)}[data-selected="true"]`).waitFor({state:"visible",timeout:3000});}
async function finishTap(frame,id,target){const z=frame.locator(zone(target)).first(),occupied=await z.locator('.duduq-dd2-item[data-dd2-item-id]').count();if(!occupied)await z.tap({force:true});else{const b=await z.boundingBox();assert(b,"tap: zone box ausente");await z.tap({force:true,position:{x:8,y:Math.max(5,b.height-8)}})}await frame.locator(`${zone(target)} ${item(id)}`).waitFor({state:"visible",timeout:3000});}
async function placeTap(frame,id,target){await selectTap(frame,id);await finishTap(frame,id,target);}
async function placeKey(frame,id,target){const x=frame.locator(item(id)).first(),z=frame.locator(zone(target)).first();await x.focus();await x.press('Enter');await frame.locator(`${item(id)}[data-selected="true"]`).waitFor({state:"visible",timeout:3000});await z.focus();await z.press('Enter');await frame.locator(`${zone(target)} ${item(id)}`).waitFor({state:"visible",timeout:3000});}
async function drag(page,from,to){const a=await from.boundingBox(),b=await to.boundingBox();assert(a&&b,'drag box ausente');await page.mouse.move(a.x+a.width/2,a.y+a.height/2);await page.mouse.down();await page.mouse.move(b.x+b.width/2,b.y+b.height/2,{steps:10});await page.mouse.up();}
async function smartWrong(page,frame,id,target){const a=frame.locator(item(id)).first(),t=frame.locator(`.duduq-dd2-target[data-dd2-target-id="${target}"]`).first(),ab=await a.boundingBox(),tb=await t.boundingBox();assert(ab&&tb,'smart box ausente');await page.mouse.move(ab.x+ab.width/2,ab.y+ab.height/2);await page.mouse.down();await page.mouse.move(tb.x-20,tb.y+tb.height/2,{steps:10});await page.mouse.up();await frame.locator(`${zone(target)} ${item(id)}`).waitFor({state:'visible',timeout:3000});}
async function toBank(page,frame,id){const a=frame.locator(item(id)).first(),b=frame.locator('.duduq-dd2-bank').first(),ab=await a.boundingBox(),bb=await b.boundingBox();assert(ab&&bb,`bank box ${id}`);await page.mouse.move(ab.x+ab.width/2,ab.y+ab.height/2);await page.mouse.down();await page.mouse.move(bb.x+bb.width-12,bb.y+bb.height-12,{steps:10});await page.mouse.up();await frame.locator(`.duduq-dd2-bank ${item(id)}`).waitFor({state:'visible',timeout:3000});}

async function mediaGeometry(page){const r=await page.evaluate(()=>{const d=document.querySelector('#mount iframe')?.contentDocument;if(!d)return null;const imgs=[...d.querySelectorAll('.duduq-dd2-item-media,.duduq-dd2-target-media')].map(i=>{const a=i.getBoundingClientRect(),p=i.parentElement?.getBoundingClientRect();return{nw:i.naturalWidth,nh:i.naturalHeight,fit:getComputedStyle(i).objectFit,a:[a.left,a.top,a.right,a.bottom,a.width,a.height],p:p?[p.left,p.top,p.right,p.bottom]:null}});const placed=[...d.querySelectorAll('.duduq-dd2-zone .duduq-dd2-item[data-dd2-item-id]')].map(n=>{const a=n.getBoundingClientRect(),h=n.closest('.duduq-dd2-target')?.querySelector('.duduq-dd2-target-head')?.getBoundingClientRect();return{a:[a.left,a.top,a.right,a.bottom],h:h?[h.left,h.top,h.right,h.bottom]:null}});return{imgs,placed,overflow:Math.max(0,d.body.scrollWidth-d.documentElement.clientWidth)}});assert(r&&r.imgs.length>=6,`MEDIA image count ${r?.imgs?.length}`);for(const x of r.imgs){assert(x.nw>0&&x.nh>0,'MEDIA image load');assert(x.fit==='contain','MEDIA object-fit');assert(x.a[4]>0&&x.a[5]>0,'MEDIA image hidden');if(x.p)assert(x.a[0]>=x.p[0]-2&&x.a[2]<=x.p[2]+2,'MEDIA image overflow card')}for(const x of r.placed)if(x.h)assert(x.a[1]>=x.h[3]-3,'MEDIA item overlaps target label');assert(r.overflow<=6,`MEDIA overflow ${r.overflow}`);}

async function runMedia(browser){const {context,page,errors,critical404}=await open(browser,'media');try{const p=media();assert(await page.evaluate(x=>dd225Validate(x),p),'MEDIA payload invalid');await mount(page,p);const frame=page.frameLocator('#mount iframe');await page.waitForFunction(()=>{const d=document.querySelector('#mount iframe')?.contentDocument,a=[...(d?.querySelectorAll('.duduq-dd2-item-media,.duduq-dd2-target-media')||[])];return a.length>=6&&a.every(i=>i.complete&&i.naturalWidth>0)},null,{timeout:20000});await mediaGeometry(page);
 const before=await snap(page);await frame.locator('.duduq-dd2-target[data-dd2-target-id="pets"] .duduq-dd2-target-audio').click({force:true});await page.waitForTimeout(80);let s=await snap(page);assert(s.results.length===0&&s.confirm===0,'MEDIA audio evaluated');assert(JSON.stringify(s.locations)===JSON.stringify(before.locations),'MEDIA audio moved item');
 await placeTap(frame,'cat','pets');await selectTap(frame,'dog');s=await snap(page);assert(s.locations.dog==='bank'&&s.results.length===0&&s.confirm===0,'MEDIA item audio moved/evaluated');await finishTap(frame,'dog','school');await placeKey(frame,'pencil','school');await smartWrong(page,frame,'backpack','pets');s=await snap(page);assert(s.results.length===0&&s.confirm===1,'MEDIA confirm-only/smart-snap broken');await mediaGeometry(page);
 await frame.locator('.duduq-dd2-confirm').click({force:true});await page.waitForFunction(()=>__DD225_RESULTS__.length===1,null,{timeout:5000});s=await snap(page);assert(s.results[0]?.isCorrect===false,'MEDIA retry missing');await page.waitForFunction(()=>{const d=document.querySelector('#mount iframe')?.contentDocument;return Boolean(d?.querySelector('.duduq-dd2-bank [data-dd2-item-id="dog"]')&&d?.querySelector('.duduq-dd2-bank [data-dd2-item-id="backpack"]')&&d?.querySelector('.duduq-dd2-zone [data-dd2-item-id="cat"]:disabled')&&d?.querySelector('.duduq-dd2-zone [data-dd2-item-id="pencil"]:disabled'))},null,{timeout:5000});
 await placeKey(frame,'dog','pets');await drag(page,frame.locator(item('backpack')).first(),frame.locator(zone('school')).first());await frame.locator(`${zone('school')} ${item('backpack')}`).waitFor({state:'visible',timeout:3000});s=await snap(page);assert(s.results.length===1&&s.confirm===1,'MEDIA correction auto-evaluated');await frame.locator('.duduq-dd2-confirm').click({force:true});await page.waitForFunction(()=>__DD225_RESULTS__.length===2&&__DD225_RESULTS__[1]?.isCorrect===true,null,{timeout:5000});await page.waitForFunction(()=>__DD225_COMPLETIONS__.length===1,null,{timeout:5000});await mediaGeometry(page);assert(!errors.length,`MEDIA JS ${errors.join('|')}`);assert(!critical404.length,`MEDIA 404 ${critical404.join(',')}`);console.log('MEDIA PASS');}finally{await context.close()}}

async function seqState(page){return page.evaluate(()=>{const d=document.querySelector('#mount iframe')?.contentDocument,slots=[...(d?.querySelectorAll('.duduq-dd2-sequence-slot')||[])];return{order:slots.map(s=>s.querySelector('[data-dd2-item-id]')?.getAttribute('data-dd2-item-id')||null),rects:slots.map(s=>{const a=s.getBoundingClientRect(),i=s.querySelector('[data-dd2-item-id]')?.getBoundingClientRect();return{a:[a.left,a.top,a.right,a.bottom,a.width,a.height],i:i?[i.left,i.top,i.right,i.bottom,i.width,i.height]:null}}),results:__DD225_RESULTS__.slice(),confirm:d?.querySelectorAll('.duduq-dd2-confirm').length||0,overflow:d?Math.max(0,d.body.scrollWidth-d.documentElement.clientWidth):999}})}
function seqLayout(s,label){assert(s.order.length===4,`${label} slots`);assert(s.overflow<=6,`${label} overflow`);for(const r of s.rects){assert(r.a[4]>20&&r.a[5]>20,`${label} hidden slot`);if(r.i){assert(r.i[4]>10&&r.i[5]>10,`${label} hidden item`);assert(r.i[0]>=r.a[0]-2&&r.i[2]<=r.a[2]+2&&r.i[1]>=r.a[1]-2&&r.i[3]<=r.a[3]+2,`${label} item outside`)}}for(let x=0;x<s.rects.length;x++)for(let y=x+1;y<s.rects.length;y++){const a=s.rects[x].a,b=s.rects[y].a,o=Math.max(0,Math.min(a[2],b[2])-Math.max(a[0],b[0]))*Math.max(0,Math.min(a[3],b[3])-Math.max(a[1],b[1]));assert(o<4,`${label} overlap ${x}/${y}`)}}
async function sequenceTap(frame,id){await selectTap(frame,id);const z=frame.locator(zone('line')).first();await z.focus();await z.press('Enter');await frame.locator(`${zone('line')} ${item(id)}`).waitFor({state:'visible',timeout:3000});}

async function runSequence(browser){const {context,page,errors,critical404}=await open(browser,'sequence');try{const p=sequence();assert(await page.evaluate(x=>dd225Validate(x),p),'SEQUENCE payload invalid');await mount(page,p);const frame=page.frameLocator('#mount iframe');await page.waitForFunction(()=>document.querySelector('#mount iframe')?.contentDocument?.querySelectorAll('.duduq-dd2-sequence-slot').length===4,null,{timeout:5000});let s=await seqState(page);seqLayout(s,'SEQUENCE initial');assert(s.confirm===0,'SEQUENCE early confirm');
 await sequenceTap(frame,'A');await sequenceTap(frame,'C');await placeKey(frame,'B','line');await drag(page,frame.locator(item('D')).first(),frame.locator(zone('line')).first());await frame.locator(`${zone('line')} ${item('D')}`).waitFor({state:'visible',timeout:3000});s=await seqState(page);assert(JSON.stringify(s.order)===JSON.stringify(['A','C','B','D']),`SEQUENCE first order ${JSON.stringify(s.order)}`);assert(!s.results.length&&s.confirm===1,'SEQUENCE pre-confirm feedback');seqLayout(s,'SEQUENCE assembled');
 await toBank(page,frame,'C');await toBank(page,frame,'B');await placeKey(frame,'B','line');await placeKey(frame,'C','line');s=await seqState(page);assert(JSON.stringify(s.order)===JSON.stringify(['A','B','C','D']),`SEQUENCE pre-confirm reposition ${JSON.stringify(s.order)}`);assert(!s.results.length,'SEQUENCE reposition evaluated');
 await toBank(page,frame,'B');await toBank(page,frame,'C');await placeKey(frame,'C','line');await placeKey(frame,'B','line');s=await seqState(page);assert(JSON.stringify(s.order)===JSON.stringify(['A','C','B','D']),`SEQUENCE retry setup ${JSON.stringify(s.order)}`);assert(!s.results.length,'SEQUENCE retry setup evaluated');await frame.locator('.duduq-dd2-confirm').click({force:true});await page.waitForFunction(()=>__DD225_RESULTS__.length===1,null,{timeout:5000});s=await seqState(page);assert(s.results[0]?.isCorrect===false,'SEQUENCE wrong not retry');await page.waitForFunction(()=>{const d=document.querySelector('#mount iframe')?.contentDocument,A=d?.querySelector('[data-dd2-item-id="A"]'),D=d?.querySelector('[data-dd2-item-id="D"]'),B=d?.querySelector('[data-dd2-item-id="B"]'),C=d?.querySelector('[data-dd2-item-id="C"]');return Boolean(A?.disabled&&D?.disabled&&!B?.disabled&&!C?.disabled)},null,{timeout:5000});
 await toBank(page,frame,'C');await toBank(page,frame,'B');await placeKey(frame,'B','line');await placeKey(frame,'C','line');s=await seqState(page);assert(JSON.stringify(s.order)===JSON.stringify(['A','B','C','D']),`SEQUENCE final order ${JSON.stringify(s.order)}`);assert(s.results.length===1&&s.confirm===1,'SEQUENCE correction auto-evaluated');seqLayout(s,'SEQUENCE corrected');await frame.locator('.duduq-dd2-confirm').click({force:true});await page.waitForFunction(()=>__DD225_RESULTS__.length===2&&__DD225_RESULTS__[1]?.isCorrect===true,null,{timeout:5000});await page.waitForFunction(()=>__DD225_COMPLETIONS__.length===1,null,{timeout:5000});assert(!errors.length,`SEQUENCE JS ${errors.join('|')}`);assert(!critical404.length,`SEQUENCE 404 ${critical404.join(',')}`);console.log('SEQUENCE PASS');}finally{await context.close()}}

const browser=await chromium.launch({headless:true});try{await runMedia(browser);await runSequence(browser)}finally{await browser.close()}console.log('PASS — Drag & Drop 2.0.25 media + sequence gaps');
