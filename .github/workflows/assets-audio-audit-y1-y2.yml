name: Assets Audio Audit Year 1-2

on:
  push:
    branches:
      - audit/assets-audio-y1-y2-20260902
    paths:
      - "test/refinement/*media-audit-y1-y2.mjs"
      - "test/refinement/assets-audio-audit-y1-y2.mjs"
      - ".github/workflows/assets-audio-audit-y1-y2.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Install Playwright Chromium once
        run: |
          npm install --no-save playwright@1.55.0
          npx playwright install --with-deps chromium
      - name: Start local server
        run: |
          python3 -m http.server 4173 --bind 127.0.0.1 > /tmp/assets-audio-http.log 2>&1 &
          for i in {1..30}; do
            if curl -fsS http://127.0.0.1:4173/ >/dev/null; then exit 0; fi
            sleep 1
          done
          cat /tmp/assets-audio-http.log
          exit 1
      - name: Static assets/audio audit
        env:
          BASE_URL: http://127.0.0.1:4173
        run: node test/refinement/assets-audio-audit-y1-y2.mjs
      - name: Rendered media classification
        env:
          BASE_URL: http://127.0.0.1:4173
        run: node test/refinement/rendered-media-audit-y1-y2.mjs
