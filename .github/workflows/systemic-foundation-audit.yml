name: Systemic Foundation Audit

on:
  push:
    branches:
      - refactor/systemic-foundation-years1-5
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  foundation-contract:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Syntax
        run: |
          node --check test/systemic/foundation-architecture-audit.mjs
          node --check test/systemic/year1-loader-compat-smoke.mjs
          node --check test/systemic/year1-public-entry-smoke.mjs
          node --check test/systemic/canary-core-progression-diagnostic.mjs
          node --check test/systemic/canary-core-1.0.11-promotion-smoke.mjs

      - name: Guard systemic foundation
        run: node test/systemic/foundation-architecture-audit.mjs

  year1-loader-compat:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install Playwright Chromium
        run: |
          npm install --no-save playwright@1.55.0
          npx playwright install --with-deps chromium

      - name: Start local server
        run: |
          python3 -m http.server 4173 --bind 127.0.0.1 > /tmp/duduq-systemic-http.log 2>&1 &
          echo $! > /tmp/duduq-systemic-http.pid
          for i in {1..30}; do
            if curl -fsS http://127.0.0.1:4173/ >/dev/null; then exit 0; fi
            sleep 1
          done
          cat /tmp/duduq-systemic-http.log
          exit 1

      - name: Smoke Year 1 M02-M06 via universal Loader
        env:
          BASE_URL: http://127.0.0.1:4173
        run: node test/systemic/year1-loader-compat-smoke.mjs

      - name: Revalidate Canary R145 Core 1.0.11 progression and completion sentinels
        env:
          BASE_URL: http://127.0.0.1:4173
        run: node test/systemic/canary-core-1.0.11-promotion-smoke.mjs

      - name: Upload final progression evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: core-r145-progression-final-evidence
          path: test-results/systemic/canary-core-1.0.11-promotion
          if-no-files-found: warn
          retention-days: 14
